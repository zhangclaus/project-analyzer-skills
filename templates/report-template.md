# <Project Name> — Analysis Report

> Generated by project-analyzer-skills on <date>

## What It Does

<1-2 paragraph summary: what problem does this project solve? who is it for? what are the core capabilities?>

| | |
|---|---|
| **Name** | <project-name> |
| **Description** | <from README> |
| **Tech Stack** | <languages, frameworks> |
| **Size** | <file count> files, ~<line count> lines |
| **License** | <license type> |

### Core Modules

> Open [architecture-explorer.html](architecture-explorer.html) to explore interactively — click modules to see full dependency chains.

| Module | Layer | WHY |
|--------|-------|-----|
| <module> | <layer> | <why it exists> |
| ... | ... | ... |

### Architecture Overview

```mermaid
graph TB
    subgraph Access["接入层"]
        <modules>
    end
    subgraph Business["业务层"]
        <modules>
    end
    subgraph Tool["工具层"]
        <modules>
    end
    subgraph Data["数据层"]
        <modules>
    end
    subgraph Infra["基础设施层"]
        <modules>
    end
    <dependency edges>
```

## How It Works

<Explain the main workflow in plain language, then show the flow diagram>

### Main Flow: <flow name>

```mermaid
flowchart TD
    <step1> --> <step2>
    <step2 --> <step3>
    ...
```

<For each significant step, briefly explain what happens and why>

## What's Innovative

<3-5 key design decisions that make this project different. Each one should explain WHY, not just WHAT.>

### 1. <Innovation title>

<2-3 paragraphs: what's the design decision? why was it chosen? what problem does it solve better than the common approach?>

### 2. <Innovation title>

...

---

## Analysis Info

- **Date:** <YYYY-MM-DD>
- **Scope:** Full project
